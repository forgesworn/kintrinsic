# Dependencies
node_modules/
package-lock.json.bak

# Build output
dist/
build/
out/
target/

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# OS
Thumbs.db

# Test coverage
coverage/
.nyc_output/

# TS incremental build
*.tsbuildinfo

# Claude Code / MCP / tooling artefacts
.claude/
.playwright-mcp/

# Native shell artefacts (Tauri / Electron — TBD)
src-tauri/target/
src-tauri/Cargo.lock
# electron-builder output at the repo root only — scripts/release/ is source
/release/

# Internal / not-for-open-source (strategy, research, private docs).
# Kept out of git so it can never leak if this repo is made public.
internal/
